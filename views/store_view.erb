<div class="jumbotron">
<h1>Welcome to <%= @store.name %></h1>
</div>


<h3> here are the current brands sold at this store. </h3>
  <%if @brands.any? %>
    <ul>
    <% @brands.each do |brand|%>
       <li><%= brand.name %></li>
    <% end %>
    </ul>
    <% else %>
    <h4>There are currently no brands supported at this store.</h4>
    <%end%>
